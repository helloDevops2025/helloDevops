name: Build Frontend Docker Image

on:
  push:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          
      - name: Clean npm cache
        run: npm cache clean --force

      - name: Install and Run dev test
        run: |
          cd ./frontend
          npm install
          npm run dev &              # 🔹 รัน dev server แบบ background
          sleep 10                   # 🔹 รอ 10 วินาทีให้ server เริ่ม
          curl -I http://localhost:5173 || true  # 🔹 ตรวจสอบว่า server ตอบได้ไหม
          pkill -f "vite"            # 🔹 หยุด process dev server
          
      # - name: Build project
      #   working-directory: ./frontend
        # run: 
        # npm run build => ถ้าเสร็จทั้งหมดแล้ว เปิดได้ทุกหน้าไม่มีปัญหาควรจะ run build ได้ ยังติด error อยู่ '=/index.html'

      - name: Build Docker image
        run: |
          docker build -t hello-devops-frontend:latest ./frontend

      # (Optional) Push image ไป Docker Hub
      # - name: Push image to Docker Hub
      #   if: success()
      #   run: |
      #     echo "${{ secrets.DOCKER_HUB_TOKEN }}" | docker login -u "${{ secrets.DOCKER_HUB_USERNAME }}" --password-stdin
      #     docker tag hello-devops-frontend:latest ${{ secrets.DOCKER_HUB_USERNAME }}/hello-devops-frontend:latest
      #     docker push ${{ secrets.DOCKER_HUB_USERNAME }}/hello-devops-frontend:latest

