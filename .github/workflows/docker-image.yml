name: Build Frontend, Run Cypress Tests snd Deploy to Docker

on:
  push:
    branches: [ main, develop ]
  workflow_dispatch:

jobs:
  e2e-test:
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: ecommerce
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h localhost -uroot -proot"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # ✅ รัน MySQL readiness check
      - name: Wait for MySQL to be ready
        run: |
          echo "⏳ Waiting for MySQL..."
          for i in {1..10}; do
            if mysql -h 127.0.0.1 -uroot -proot -e "SELECT 1" >/dev/null 2>&1; then
              echo "✅ MySQL is ready!"
              exit 0
            fi
            echo "❌ Retry ($i/10)..."
            sleep 5
          done
          exit 1
          
      - name: Login to Docker Hub
        run: echo ${{ secrets.DOCKER_TOKEN }} | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin

      # ✅ ดึง backend จาก Docker Hub
      - name: Pull backend image
        run: docker pull u6687032/hello-devops-backend:latest

      # ✅ รัน backend container
      - name: Run backend container
        run: |
          docker run -d --name backend \
            -p 8080:8080 \
            -e SPRING_DATASOURCE_URL=jdbc:mysql://127.0.0.1:3306/testdb \
            -e SPRING_DATASOURCE_USERNAME=root \
            -e SPRING_DATASOURCE_PASSWORD=root \
            u6687032/hello-devops-backend:latest

      # ✅ Install Node.js + dependencies
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install frontend dependencies
        working-directory: ./frontend
        run: |
          ls
          pwd
          npm install

      # # ✅ Run Cypress tests (frontend + backend พร้อม)
      # - name: Run Cypress E2E tests
      #   uses: cypress-io/github-action@v6
      #   with:
      #     working-directory: ./frontend
      #     start: npm run dev
      #     wait-on: 'http://localhost:5173'
      #     browser: chrome
      #     headless: true

  # build:
  #  runs-on: ubuntu-latest
  #  needs: e2e-test
  #  steps:
  #     # ✅ Build frontend for deploy
  #     - name: Build frontend
  #       working-directory: ./frontend
  #       run: npm run build
        
  Docker:
   runs-on: ubuntu-latest
   needs: e2e-test
   outputs:
      TAG: ${{ steps.set_tag.outputs.TAG }}   # ✅ ส่งค่าออกไปให้ job ต่อไปใช้ได้
      
   steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Generate package-lock.json if missing
        working-directory: ./frontend
        run: |
          if [ ! -f package-lock.json ]; then
            npm install
          fi
      
      # ตั้ง tag จาก commit SHA
      - name: Set build tag
        id: set_tag
        run: |
          TAG=${GITHUB_SHA::7}
          echo "TAG=$TAG" >> $GITHUB_ENV
          echo "TAG=$TAG" >> $GITHUB_OUTPUT

      - name: Debug TAG before build
        run: echo "TAG value is ${{ env.TAG }}"

      # Build Docker image
      - name: Build Docker image
        run: |
          cd frontend
          docker build -t u6687032/hello-devops-frontend:${{ env.TAG }} --build-arg MODE=production  . 

      # 🔐 Login Docker Hub
      - name: Login to Docker Hub
        run: echo ${{ secrets.DOCKER_TOKEN }} | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin

      # 📦 Push image และเก็บ log
      - name: Push image to Docker Hub
        if: success()
        run: |
          echo "🚀 Pushing image to Docker Hub..." 
          docker push u6687032/hello-devops-frontend:${{ env.TAG }}
          docker tag u6687032/hello-devops-frontend:${{ env.TAG }} u6687032/hello-devops-frontend:latest
          docker push u6687032/hello-devops-frontend:latest
  Deploy:
   runs-on: ubuntu-latest
   needs: Docker
   env:
      TAG: ${{ needs.Docker.outputs.TAG }}   # ✅ ดึงค่าจาก job build
      PROJECT_ID: hellodevops-476816
      CLUSTER_NAME: hello-devops-cluster
      REGION: asia-northeast1
      DEPLOYMENT_NAME: backend
      
   steps:

      - name: Checkout repository
        uses: actions/checkout@v4
        
      # 1️⃣ Authenticate to Google Cloud
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}
  
      # 2️⃣ Get GKE credentials (เชื่อมต่อกับ cluster)
      - name: Get GKE credentials
        uses: google-github-actions/get-gke-credentials@v2
        with:
          cluster_name: ${{ env.CLUSTER_NAME }}
          location: ${{ env.REGION }}
          project_id: ${{ env.PROJECT_ID }}
          
      # ✅ 3. สร้าง/อัปเดต ConfigMap จาก init.sql (ฐานข้อมูล)
      - name: Update MySQL init ConfigMap
        run: |
          kubectl delete configmap mysql-initdb --ignore-not-found
          kubectl create configmap mysql-initdb --from-file=init.sql=./init.sql
          kubectl get configmap mysql-initdb

      - name: Debug TAG value
        run: 'echo "🔎 Current TAG value: ${{ env.TAG }}"'
        
      # Update image to specific version (ถ้ามี tag เฉพาะ)
      - name: Update Deployment frontend images
        if: env.TAG != ''
        run: |
          echo "🔄 Updating deployments to tag: ${{ env.TAG }}"
          kubectl set image deployment/frontend frontend=u6687032/hello-devops-frontend:${{ env.TAG }}
          kubectl rollout status deployment/frontend

  
